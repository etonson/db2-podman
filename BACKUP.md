# DB2 備份與還原規範

本專案採用結構化的備份管理方式，讓開發與測試環境可以透過備份檔快速重建，
也可以把容器中已修改的資料庫重新備份回本機。設計沿用 `sqlserverForEris` 的做法。

## 備份目錄結構

所有備份都放在 `backups/{database}/`。`docker-compose.yml` 會把本機 `./backups`
掛載到容器內的 `/database/backups`，因此容器內的備份與還原都會直接對應到本機檔案。

```text
backups/
├── PTPMSDB/
│   ├── PTPMSDB.0.db2inst1.DBPART000.20260903070931.001   # DB2 備份檔
│   └── metadata.json                                      # 備份中繼資料
└── .gitkeep
```

與 SQL Server 不同的是，**DB2 的備份檔名由 DB2 自己決定**，不能改名成 `latest.bak`：
還原時的 `TAKEN AT {timestamp}` 必須對應檔名裡的時間戳。因此這裡不做 `latest` 歸檔，
而是讓同一個目錄下自然累積多份帶時間戳的備份，還原時取最新的一份。

## Metadata 規範

每個備份目錄下會有一個 `metadata.json`：

```json
{
  "database": "PTPMSDB",
  "db2Version": "12.1",
  "source": "backup.sh",
  "timestamp": "20260903070931",
  "createdAt": "2026-09-03",
  "description": "Backup of PTPMSDB database generated by backup.sh"
}
```

`timestamp` 就是 DB2 的備份時間戳，方便人工比對要還原哪一份。

## 自動還原機制

容器啟動時，官方 entrypoint 會執行 `/var/custom/restore-databases.sh`（原始檔在 `custom/`），邏輯如下：

1. **特定還原**：若環境變數 `RESTORE_DB` 有值（例如 `RESTORE_DB=PTPMSDB`），則僅還原該資料庫。
2. **全量還原**：若 `RESTORE_DB` 為空，則掃描 `/database/backups/*/`，依序還原。
3. **跳過機制**：若資料庫已存在，則跳過該資料庫，避免覆蓋既有資料。
4. **目錄名即資料庫名**：目標資料庫名稱取自目錄名，與備份檔內記錄的來源名稱不同時，
   會自動用 `RESTORE ... INTO {目錄名}` 改名還原。
5. **自動收尾**：改名還原會讓資料庫進入 `ROLL-FORWARD PENDING`，
   腳本會自動執行 `ROLLFORWARD ... TO END OF BACKUP AND COMPLETE`，否則連不上。

### 手動觸發特定還原

```bash
RESTORE_DB=PTPMSDB podman-compose up -d
```

## 資料庫備份腳本

`backup.sh` 可將運行中的 DB2 資料庫備份回宿主機的 `backups/` 目錄。

### 前置條件

1. `.env` 內有正確的 `DBNAME`。
2. `db2-gtky` 容器已啟動。

### 使用方式

1. **互動式選單備份**：不帶參數執行，會列出容器內所有資料庫供選擇，
   也可選擇全部備份或取消：

   ```bash
   ./backup.sh
   ```

2. **指定資料庫備份**：

   ```bash
   ./backup.sh PTPMSDB
   ```

### 備份流程

1. 載入 `.env` 中的連線資訊。
2. 自動偵測目前使用 `podman` 或 `docker`。
3. 檢查 `db2-gtky` 容器是否正在執行。
4. 用 `db2 list db directory` 取得資料庫清單。
5. 依照參數或互動選單決定備份目標。
6. 建立 `backups/{database}` 目錄。
7. 斷開所有連線並停用資料庫，執行 `BACKUP DATABASE ... COMPRESS`。
8. 從備份輸出解析時間戳，產生或更新 `metadata.json`。

## 注意事項

* **只能離線備份**：目前實例使用循環日誌（circular logging），DB2 不允許線上備份。
  `backup.sh` 會先 `FORCE APPLICATIONS ALL` 踢掉所有連線，
  **執行前請確認沒有應用程式正在使用資料庫**。若要線上備份，需先改成保存日誌（archive logging）。
* **資料庫已存在時不覆蓋**：自動還原時若目標資料庫已存在，腳本會跳過。
  要重新還原必須先 `DROP DATABASE`，或清掉 `db2_storage/` 重建容器。
* **備份檔不進版控**：`backups/*/` 下的備份檔動輒上百 MB，`.gitignore` 只保留 `metadata.json`。
* **權限**：備份檔在容器內屬於 `db2inst1`，在宿主機上會顯示成 subuid（例如 `100999`）。
  要在宿主機搬動或刪除，請用 `podman unshare`。
