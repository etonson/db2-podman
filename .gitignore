# 暫存檔案
*.tmp
*.swp
*~
# log 檔
*.log
# 建置產物
build/
target/

# 舊版 bind mount 設計留下的資料庫目錄，已改用具名 volume，可直接刪除
db2_storage/

# 資料庫備份檔動輒上百 MB，只保留目錄結構與 metadata
backups/*/*
!backups/*/metadata.json

# 注意：.env 只存開發環境用的密碼，刻意不忽略（見 README）
